package #{package}.#{name-lower}.commands;

import org.bukkit.command.CommandSender;

import #{package}.#{name-lower}.AbstractCommand;

public class #{command-name}Command extends AbstractCommand {

	public #{command-name}Command() {
		#{extras}
	}

	@Override
	public void execute(CommandSender sender, String[] args) {
		// TODO
	}

}